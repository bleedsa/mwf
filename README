MOB WITH FRIENDS

DEPS

	* libtool
	* autotools
	* ninja
	* cmake
	* rlwrap
	* OpenGL
	* libboost-dev
	* libx11-dev
	* libxrandr-dev
	* libxinerama-dev
	* libxcursor-dev
	* libxi-dev
	* libopengl-dev

BUILDING

	% git submodule init && git submodule update
	% for f in etc/mk/*; do $f; done
	% etc/ninja.k>build.ninja
	% ninja
